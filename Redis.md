# Redis: key-value база данных

* За счет чего обеспечивается высокая скорость чтения и записи в `Redis`?

* `Redis` может хранить данные на диске?

* Как в `Redis` реализовыввается персистентность данных?

* Какие типы данных поддерживаются в `Redis`?

* Какие индексы поддерживаются в `Redis`?

* Какая модель обслуживания клиентов в `Redis`? `Redis` использует потоки системы?

* Как в `Redis` реализована репликация данных от мастера к репликам?

* Как настроить публикацию и подписку для репликации данных?
