## Общее

* Знать "на зубок" модули
[`bufio`](https://pkg.go.dev/bufio)
[`builtin`](https://pkg.go.dev/builtin)
[`cmp`](https://pkg.go.dev/cmp)
[`context`](https://pkg.go.dev/context)
[`errors`](https://pkg.go.dev/errors)
[`fmt`](https://pkg.go.dev/fmt)
[`io`](https://pkg.go.dev/io)
[`maps`](https://pkg.go.dev/maps)
[`math/rand`](https://pkg.go.dev/math/rand)
[`math`](https://pkg.go.dev/math)
[`net/http`](https://pkg.go.dev/net/http)
[`net/url`](https://pkg.go.dev/net/url)
[`net`](https://pkg.go.dev/net)
[`os`](https://pkg.go.dev/os)
[`reflect`](https://pkg.go.dev/reflect)
[`regexp`](https://pkg.go.dev/regexp)
[`runtime`](https://pkg.go.dev/runtime)
[`slices`](https://pkg.go.dev/slices)
[`strconv`](https://pkg.go.dev/strconv)
[`strings`](https://pkg.go.dev/strings)
[`sync`](https://pkg.go.dev/sync)
[`sync/atomic`](https://pkg.go.dev/sync/atomic)
[`sync/errgroup`](https://pkg.go.dev/golang.org/x/sync/errgroup)
[`time`](https://pkg.go.dev/time)
[`unsafe`](https://pkg.go.dev/unsafe)

* Какие достоинства и недостатки `Go` вы знаете? Почему `Go` не подходит для встраиваемых систем?

* В `Go` есть поддержка ООП? Встраивание является наследованием? Использование интерфейсов обеспечивает полиморфизм? Как инкапсулировать функции, методы и переменные в модулях?

* Что такое интерфейс? Что такое пустой интерфейс и когда его используют? Что собой представляет тип данных `any`?

* `Go` интепретируемый или компилируемый язык?

* В чем отличия сборки и выполения в `Go` и, например, в `C` и `Java`?

## Типы данных

### Каналы

* Что такое каналы? Каких выидов они бывают?

* Отличия буферизированного канала от небуферизированного.

* Какой размер буфера по умолчанию у канала?

* Какое значение вернется из закрытого канала?

* Что произойдет при записи значения в закрытый канал?

* Что произойдет при записи в непроинициализированный канал?

### Слайсы и массивы

* Что происходит с `capacity` слайса и базовым массивом после добавления элементов через `append()`?

* Как создать многомерный слайс?

* Какие базовые типы данных в `Go` знаете?

* Функции в `Go` считаются типами?

* Как создать литералы слайсов, `map` и структур?

* Что вернет функция `len()` с аргументом типа строка? Что собой представляет тип данных `rune`?	

* Какими типами ограничены ключи в `map`?

* Опишите внутренную структуру небазовых типов в `Go`: `массив` `слайс` `строка` `map` `interface` `канал`.

* Почему перед использованием слайс нужно проинициализировать, а массив нет? А `map` почему необязательно инициализировать?

* Какие операции над непроинициализированным слайсом можно провести?

* Зачем в `Go` заключают куски кода в фигурные скобки?
* Как обновить значения полей объекта в одной строке кода?


## Внутреннее устройство

* В какой части `Go`-программы хранятся переменные? Как `Go` использует `heap`?

* Что такое метод структуры? В чем разница между ресивером по значению и по указателю?


## Горутины и многозадачность

* В чем отличие горутин от потоков?

* Можно ли вытеснить горутину из очереди исполнения? Можно ли остановить выполняющуюся горутину? Какой инструмент в `Go` можно приспособить для завершения горутин?

* В чем отличия работы системного и `Go` планировщиков? Опишите алгоритм работы планировщика `Go` в терминах `GMP`.

* Какой `memory footprint` у горутин?

* Есть ли в `Go` структуры типа `lock free`? Что собой представляет `sync.Map` и типы из модуля `sync/atomic`? За счет чего атомики такие быстрые?

* Отличият `sync.RWMutex` от `sync.Mutex`.

* Когда возникает состояние гонки данных? Как в `Go` избегают таких состояний?

* Паттерны `Fan-In` и `Fan-Out`, уметь реализовать их в коде.

* Когда следует использовать `errgroup`?

## Память

* Как устроено управление памятью в `Go`? Каков размер стека функций и как он растет?

* Как происходит выравнивание памяти в структурах? Как лучше организовать поля структур чтобы уменьшить размер объектов?

## Базы данных

* Преимущества и недостатки `SQL` и `ORM`. Какие `ORM` библиотеки в `Go` знаешь?


## Функции

* Как в `Go` реализована поддержка замыканий?

* Почему `context.Context` принято передавать первым аргументов в функции?

* В каком случае для `generic` функций не нужно указывать тип?


## Интерфейсы

* Как использование интерфейсов в `Go` влияет на `coupling` и `code reuse`?

* Использование интерфейсов реализует контрактное программирование?

## Контексты

* Почему `context.Context` не рекомендуется встраивать в структуры? Каков срок жизни `context.Context`?

* Почему не рекомендуется передавать `nil` для неизвестного контекста? Для чего используют `context.TODO()`?

* Почему для `context.WithCancel()`, `context.WithTimeout()` и `context.Deadline()` лучше использовать `defer cancel()`?

* Почему не рекомендуется использовать `context.WithValue()` для передачи больших объектов?

* Почему вместо `context.Background()` в обработчиках запросов рекомендуется использовать родительский контекст?

* Как необходимо обрабатывать `ctx.Done()` в `select`?


## gRPC

* Какие режимы работы `gRPC` существуют? Когда следует использовать унарный режим? А когда потоковый (серверный, клиентский, двунаправленный) режим?

## Производительность

* Какие метрики нужно использовать для профилирования программы на `Go`?


* Нужно ли передавать слайс по ссылке?

* В чем отличие ресивера по значению и по ссылке в методах?

* Как в `Go` работает сборка мусора? Можно ли ее отключить? Когда это может потребоваться?


* Возможно ли ограничить максимальное число горутин?

* Почему важно завершать горутины по окончании и/или прерывании работы программы?


* `Go` императивный или декларативный язык?

* Что такое `type switch` и когда он используется?

* Как сообщить компилятору что пользовательский тип реализует интерфейс?

* Как отсортировать массив структур по алфавиту по полю `Name`?

* Что такое сериализация и когда она используется?

* Где следует разместить описание интерфейса: в пакете с реализацией или в пакете, где этот интерфейс используется?

* Какие надостатки стандартного логгера знаете?

* Какие `ORM` для `Go` знаете? Какие плюсы и минусы каждого?

* Какие линтеры для `Go` знаете?

* Можно ли использовать один и тот же буфер типа `[]byte` в нескольких горутинах?

* Как оценивать производительность программы на `Go` в проде?

* Какие метрики в `Prometheus` для `Go` программы вы реализуете в первую очередь?

* Как встроить стандартный профайлер в `Go` приложение? Какой `overhead` он создает?

* Какие средства обобщенного программирования существуют в `Go`?

* Как в `Go` реализовать пользовательский примитив для управления памятью?

* Как работает Сборщик Мусора в `Go`? Когда `runtime` запускает его?

* Какие недостатки у подхода сборки мусора `Mark-and-Sweep`?

* Для чего используются переменные окружения `GO` и `GODEBUG`?

* Что такое `router` и `middleware` в `Go`?

* Почему `append()` для непроинициализи  рованного слайса отрабатывает, а при записи ключа в непроинициализированный `map` программа паникует?

* Когда итератор `range` создает копии?
