# Вопросы PostgreSQL

* Что такое транзакция? Возможно ли частиночное исполнение транзакции? Возможно ли откатить транзацию?

* Какие типы данных поддерживает `PostgreSQL`?

* Какие индексы знаете и как они устроены?

* В чем отличия индексов на бинарных деревьях и хеш-таблицах?

* Какие методы доступа к индексам знаете?

* В чем отличия методов доступа к индексам `GiST`, `SP-GiST`, `GIN` и `BRIN`?

* Что такое изоляция данных и какие виды знаете?

* Как устроена шардирование и репликация?

* Рассшифруйте аббревиатуру `ACID`.

* Как составляется план запросов? Команда `EXPLAIN`.

* Какие типы `View` существуют?

* Что такое `Common Table Expression` и как оно используется?

* Как реализуется изоляция транзакций через `MVCC`?

* Какие уровни изоляции транзакций существуют?

* В чем отличия уровней изоляции `Read Uncommitted`, `Read Committed`, `Repeatable Read` и `Serializable`?

* `PostgreSQL` использует потоки или процессы?

* Как работает асинхронная репликация между нодами? Как `WAL` логи используются при асинхронной репликации?
