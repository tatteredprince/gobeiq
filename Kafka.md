# Kafka: брокер сообщений

* Отличия `push` и `pull` моделей.

* Гарантии доставки: `at least once` и `at most once`.

* Для чего используются `consumer group`?

* Какие типы топиков существуют? Как истекает срок записей?

* Какие `API` поддерживюатся?

* Как работают партиции?

* Как работает ребалансировка?
